#define a function named display_message with one parameter, message
def display_message(message):
    print(message)

#set a string to a variable called msg
msg = "I'm learning to store code in functions."

#call the display_message function with the msg variable as argument
display_message(msg)
